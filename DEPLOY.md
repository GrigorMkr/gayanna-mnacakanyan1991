# 🚀 Как запустить сайт и поделиться ссылкой

## 📍 Локальный запуск (для разработки)

### Быстрый старт:

1. **Установите зависимости** (если еще не установлены):
   ```bash
   npm install
   ```

2. **Запустите dev-сервер**:
   ```bash
   npm start
   ```
   
   Или:
   ```bash
   npm run dev
   ```

3. **Сайт автоматически откроется** в браузере по адресу:
   ```
   http://localhost:8080
   ```

### ⚠️ Важно:
- Это **локальный адрес** - он работает только на вашем компьютере
- Друзья **не смогут** открыть этот адрес со своих компьютеров
- Для того чтобы поделиться ссылкой, нужно разместить сайт в интернете

---

## 🌐 Размещение сайта в интернете (чтобы поделиться ссылкой)

Есть несколько способов разместить сайт в интернете:

### 1. GitHub Pages (Бесплатно) ⭐ Рекомендуется

**Преимущества:** Бесплатно, легко настроить, автоматическое обновление

#### Шаги:

1. **Соберите проект для production:**
   ```bash
   npm run build
   ```
   Это создаст папку `dist/` с готовыми файлами

2. **На GitHub:**
   - Откройте репозиторий: https://github.com/GrigorMkr/gayanna-mnacakanyan1991
   - Перейдите в **Settings** (Настройки)
   - В левом меню найдите **Pages**
   - В разделе **Source** выберите:
     - Branch: `main`
     - Folder: `/dist` (или создайте ветку `gh-pages`)
   - Нажмите **Save**

3. **Ваш сайт будет доступен по адресу:**
   ```
   https://grigormkr.github.io/gayanna-mnacakanyan1991/
   ```
   (или другой адрес, который укажет GitHub)

#### Автоматизация через GitHub Actions:

Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

---

### 2. Netlify (Бесплатно) ⭐ Очень просто

**Преимущества:** Очень просто, автоматический деплой, свой домен

#### Шаги:

1. **Соберите проект:**
   ```bash
   npm run build
   ```

2. **Зарегистрируйтесь на Netlify:**
   - Перейдите на https://www.netlify.com
   - Войдите через GitHub

3. **Добавьте сайт:**
   - Нажмите **"Add new site"** → **"Import an existing project"**
   - Выберите ваш репозиторий `gayanna-mnacakanyan1991`
   - Настройки сборки:
     - **Build command:** `npm run build`
     - **Publish directory:** `dist`
   - Нажмите **"Deploy site"**

4. **Ваш сайт будет доступен по адресу:**
   ```
   https://gayanna-mnacakanyan1991.netlify.app
   ```
   (или другой адрес, который даст Netlify)

5. **Можно настроить свой домен** в настройках сайта

---

### 3. Vercel (Бесплатно)

**Преимущества:** Быстро, автоматический деплой, оптимизация

#### Шаги:

1. **Зарегистрируйтесь на Vercel:**
   - Перейдите на https://vercel.com
   - Войдите через GitHub

2. **Добавьте проект:**
   - Нажмите **"Add New Project"**
   - Выберите репозиторий `gayanna-mnacakanyan1991`
   - Настройки:
     - **Framework Preset:** Other
     - **Build Command:** `npm run build`
     - **Output Directory:** `dist`
   - Нажмите **"Deploy"**

3. **Ваш сайт будет доступен по адресу:**
   ```
   https://gayanna-mnacakanyan1991.vercel.app
   ```

---

### 4. Surge.sh (Бесплатно, через командную строку)

**Преимущества:** Быстро, просто, через терминал

#### Шаги:

1. **Установите Surge:**
   ```bash
   npm install -g surge
   ```

2. **Соберите проект:**
   ```bash
   npm run build
   ```

3. **Запустите деплой:**
   ```bash
   cd dist
   surge
   ```
   
   При первом запуске нужно будет:
   - Зарегистрироваться (создать аккаунт)
   - Выбрать домен (например: `gayanna-mnacakanyan1991.surge.sh`)

4. **Ваш сайт будет доступен по адресу:**
   ```
   https://gayanna-mnacakanyan1991.surge.sh
   ```

---

## 📝 Рекомендации

### Для быстрого старта:
- **Netlify** или **Vercel** - самый простой способ, автоматический деплой при каждом коммите

### Для полного контроля:
- **GitHub Pages** - если хотите все хранить в одном месте

### Для простоты:
- **Surge.sh** - если хотите быстро задеплоить через терминал

---

## 🔄 Обновление сайта

После того как сайт размещен:

1. **Внесите изменения** в код
2. **Сделайте коммит и отправьте на GitHub:**
   ```bash
   git add .
   git commit -m "Обновление сайта"
   git push
   ```
3. **Сайт автоматически обновится** (если используете Netlify/Vercel/GitHub Actions)

---

## 📱 Поделиться ссылкой

После размещения сайта вы получите постоянную ссылку, например:
- `https://gayanna-mnacakanyan1991.netlify.app`
- `https://grigormkr.github.io/gayanna-mnacakanyan1991/`

Эту ссылку можно:
- ✅ Отправить друзьям
- ✅ Разместить в социальных сетях
- ✅ Добавить в резюме
- ✅ Использовать как портфолио

---

## ⚙️ Настройка для production

Перед размещением убедитесь, что в `webpack.config.js` правильно настроены пути к изображениям и другим ресурсам.

Если изображения не загружаются, проверьте:
- Пути к изображениям в `src/config/appConfig.js` должны начинаться с `/images/`
- В `webpack.config.js` настроен `CopyWebpackPlugin` для копирования папки `images`

---

## 🆘 Проблемы?

Если что-то не работает:
1. Проверьте, что проект собирается локально: `npm run build`
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что все пути к ресурсам правильные
4. Проверьте логи деплоя на платформе

