import './styles.css';

/**
 * Менеджер стилей приложения
 */
export class StyleManager {
    constructor(colors) {
        this.colors = colors;
    }

    /**
     * Инициализация стилей
     */
    init() {
        // CSS стили теперь подключены через импорт в index.js
        // Этот метод оставлен для совместимости
    }
}
