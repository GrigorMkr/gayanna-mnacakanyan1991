/**
 * Презентер для секции "О художнице"
 */
export class AboutPresenter {
    constructor(config) {
        this.config = config;
    }

    /**
     * Получает конфигурацию
     * @returns {Object}
     */
    getConfig() {
        return this.config;
    }
}

