# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ: GitHub Pages показывает README вместо сайта

## Проблема
GitHub Pages показывает README.md вместо собранного сайта.

## ✅ РЕШЕНИЕ (выполните эти шаги):

### Шаг 1: Проверьте настройки GitHub Pages

1. Откройте: https://github.com/GrigorMkr/gayanna-mnacakanyan1991/settings/pages

2. В разделе **"Source"** должно быть:
   - ✅ **"GitHub Actions"** (если доступно)
   
   ИЛИ если нет опции GitHub Actions:
   - ✅ **"Deploy from a branch"**
   - Branch: `main` или `gh-pages`
   - Folder: `/dist` или `/ (root)`

3. **ВАЖНО:** Если там написано "None" или "README" - это неправильно!

4. Нажмите **Save**

### Шаг 2: Проверьте GitHub Actions

1. Откройте: https://github.com/GrigorMkr/gayanna-mnacakanyan1991/actions

2. Должен быть workflow "Deploy to GitHub Pages"
3. Если он красный (failed) - откройте его и посмотрите ошибки
4. Если его нет - подождите 1-2 минуты после настройки Pages

### Шаг 3: Если GitHub Actions не работает

Выполните вручную через терминал:

```bash
# Убедитесь что вы в корне проекта
cd c:\Users\Home\Desktop\gayana

# Соберите проект
npm run build

# Создайте ветку gh-pages
git checkout -b gh-pages

# Скопируйте файлы из dist в корень
# В Windows PowerShell:
Copy-Item -Path dist\* -Destination . -Recurse -Force

# Закоммитьте
git add .
git commit -m "Deploy to GitHub Pages"

# Отправьте
git push origin gh-pages

# Вернитесь на main
git checkout main
```

Затем в Settings → Pages выберите:
- Source: Branch `gh-pages`
- Folder: `/ (root)`

### Шаг 4: Очистите кеш браузера

После настройки:
1. Откройте сайт: https://grigormkr.github.io/gayanna-mnacakanyan1991/
2. Нажмите **Ctrl+Shift+R** (или Cmd+Shift+R на Mac) для жесткой перезагрузки
3. Или откройте в режиме инкогнито

## 🔍 Проверка

После выполнения всех шагов:
- Подождите 2-3 минуты
- Откройте: https://grigormkr.github.io/gayanna-mnacakanyan1991/
- Должен открыться сайт с галереей картин, а НЕ README.md

## ❓ Если всё ещё не работает

1. Проверьте, что файл `.github/workflows/deploy.yml` есть в репозитории
2. Проверьте, что папка `dist/` содержит файлы после сборки
3. Убедитесь, что в Settings → Pages выбран правильный source

