# 🔧 Настройка GitHub Pages для сайта

## Проблема
GitHub Pages показывает README.md вместо сайта. Это происходит потому, что GitHub Pages нужно указать, откуда брать файлы сайта.

## ✅ Решение: Настройка через GitHub Actions

Я создал автоматический деплой через GitHub Actions. Теперь нужно настроить GitHub Pages:

### Шаги:

1. **Откройте репозиторий на GitHub:**
   https://github.com/GrigorMkr/gayanna-mnacakanyan1991

2. **Перейдите в Settings (Настройки):**
   - Нажмите на вкладку **Settings** в верхнем меню репозитория

3. **Найдите раздел Pages:**
   - В левом меню прокрутите вниз и найдите **Pages**

4. **Настройте Source:**
   - В разделе **Source** выберите:
     - **Deploy from a branch** → измените на **GitHub Actions**
   - Или если опции "GitHub Actions" нет:
     - Выберите **Deploy from a branch**
     - Branch: `gh-pages` или `main`
     - Folder: `/ (root)` или `/dist`
     - Нажмите **Save**

5. **Если используете GitHub Actions:**
   - После первого коммита с workflow файлом (`.github/workflows/deploy.yml`)
   - GitHub автоматически создаст environment "github-pages"
   - Сайт будет автоматически деплоиться при каждом push в main

### Альтернативный способ (если GitHub Actions не работает):

1. **Создайте ветку gh-pages:**
   ```bash
   git checkout -b gh-pages
   ```

2. **Скопируйте файлы из dist в корень:**
   ```bash
   cp -r dist/* .
   ```

3. **Закоммитьте и отправьте:**
   ```bash
   git add .
   git commit -m "Deploy to GitHub Pages"
   git push origin gh-pages
   ```

4. **В настройках GitHub Pages:**
   - Source: Branch `gh-pages`
   - Folder: `/ (root)`

## 🔄 Автоматический деплой

После настройки GitHub Actions:
- Каждый раз когда вы делаете `git push`, сайт автоматически собирается и деплоится
- Не нужно вручную запускать `npm run build`
- Сайт обновляется автоматически

## 📍 Проверка

После настройки подождите 1-2 минуты и откройте:
```
https://grigormkr.github.io/gayanna-mnacakanyan1991/
```

Если видите сайт с галереей картин - всё работает! ✅

Если всё ещё видите README.md - проверьте настройки Pages еще раз.

## 🆘 Если не работает

1. Проверьте, что workflow файл `.github/workflows/deploy.yml` есть в репозитории
2. Проверьте Actions вкладку на GitHub - там должны быть запуски workflow
3. Убедитесь, что в Settings → Pages выбран правильный source

