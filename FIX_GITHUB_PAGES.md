# 🔧 Исправление проблемы с GitHub Pages

## Проблема
GitHub Pages показывает README.md вместо сайта.

## ✅ Решение

### Шаг 1: Проверьте настройки GitHub Pages

1. Откройте репозиторий: https://github.com/GrigorMkr/gayanna-mnacakanyan1991
2. Перейдите в **Settings** → **Pages**
3. Убедитесь, что выбрано:
   - **Source:** `Deploy from a branch` → измените на **`GitHub Actions`**
   
   ИЛИ если опции GitHub Actions нет:
   - **Source:** `Deploy from a branch`
   - **Branch:** `gh-pages` (если есть) или `main`
   - **Folder:** `/dist` или `/ (root)`

### Шаг 2: Проверьте GitHub Actions

1. Перейдите во вкладку **Actions** на GitHub
2. Проверьте, что workflow "Deploy to GitHub Pages" выполнился успешно (зеленый статус)
3. Если есть ошибки - откройте workflow и посмотрите логи

### Шаг 3: Подождите

После настройки подождите 1-2 минуты, пока GitHub обработает изменения.

### Шаг 4: Проверьте сайт

Откройте: https://grigormkr.github.io/gayanna-mnacakanyan1991/

Если всё ещё видите README.md:
- Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
- Попробуйте открыть в режиме инкогнито

## 🔄 Если GitHub Actions не работает

Альтернативный способ - деплой через ветку gh-pages:

```bash
# Создайте ветку gh-pages
git checkout -b gh-pages

# Скопируйте файлы из dist в корень
cp -r dist/* .

# Закоммитьте
git add .
git commit -m "Deploy to GitHub Pages"

# Отправьте
git push origin gh-pages
```

Затем в Settings → Pages выберите:
- Source: Branch `gh-pages`
- Folder: `/ (root)`

## 📝 Важно

После каждого изменения кода:
1. Сделайте коммит и push
2. GitHub Actions автоматически соберет и задеплоит сайт
3. Подождите 1-2 минуты
4. Обновите страницу сайта

