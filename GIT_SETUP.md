# Инструкция по добавлению проекта в Git/GitHub

## ✅ Что уже сделано:

1. ✅ Git репозиторий инициализирован
2. ✅ Все файлы добавлены в staging
3. ✅ Первый коммит создан

## 📝 Следующие шаги:

### 1. Добавить удаленный репозиторий на GitHub

Если у вас еще нет репозитория на GitHub:

1. Зайдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"New repository"** (или перейдите по ссылке: https://github.com/new)
3. Введите название репозитория (например: `gayana-artist-website`)
4. Выберите **Public** или **Private**
5. **НЕ** ставьте галочки на "Initialize with README", "Add .gitignore", "Choose a license" (так как у нас уже есть эти файлы)
6. Нажмите **"Create repository"**

### 2. Подключить локальный репозиторий к GitHub

После создания репозитория на GitHub, выполните команды (замените `YOUR_USERNAME` и `REPOSITORY_NAME` на ваши данные):

```bash
# Добавить remote репозиторий
git remote add origin https://github.com/YOUR_USERNAME/REPOSITORY_NAME.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/REPOSITORY_NAME.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код на GitHub
git push -u origin main
```

### 3. Проверка

После выполнения команд, обновите страницу репозитория на GitHub - вы должны увидеть все файлы проекта.

## 🔄 Работа с Git в будущем

### Основные команды:

```bash
# Проверить статус изменений
git status

# Добавить все изменения
git add .

# Или добавить конкретный файл
git add src/styles/styles.css

# Создать коммит
git commit -m "Описание изменений"

# Отправить изменения на GitHub
git push

# Получить изменения с GitHub
git pull
```

### Полезные команды:

```bash
# Посмотреть историю коммитов
git log

# Посмотреть изменения в файле
git diff

# Отменить изменения в файле (до добавления в staging)
git checkout -- filename

# Посмотреть информацию о remote репозитории
git remote -v
```

## 📌 Важные замечания:

1. **Не коммитьте** файлы из папки `node_modules/` и `dist/` - они уже в `.gitignore`
2. **Всегда делайте коммиты** с понятными сообщениями
3. **Делайте `git pull`** перед `git push`, если работаете с нескольких компьютеров
4. **Регулярно делайте коммиты** - это поможет отслеживать изменения

## 🚀 Готово!

Теперь ваш проект готов к работе с Git и GitHub!

